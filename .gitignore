# Binaries
bin/
!npm-package/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go
vendor/
*.test
*.out
coverage.txt

# TLS dev certs
*.pem
*.key
*.crt
*.csr

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Node (web/)
web/node_modules/
web/.next/
web/out/

# SDK
sdk-js/node_modules/
sdk-js/dist/
sdk-python/__pycache__/
sdk-python/dist/
sdk-python/*.egg-info/

# Environment
.env
.env.local
dist/
.env
certs/
*.pem
sdk-python/.venv/
web/.next/
web/node_modules/
go.work.sum
INFRASTRUCTURE.md
PROGRESS.md

# Local secrets mirror
secrets/

# Operations runbook (contains real credentials)
RUNBOOK.md
.bin/
SESSION-LOG-*.md

# Session/local + build artifacts (added during rebrand)
.claude/
serverme-linux
servermesrv-new
cli/dist/
web/--no-xattrs

# Build artifacts and deps that slipped through the per-directory rules above.
# node_modules was only ignored for web/ and sdk-js/, so video/ was leaking
# ~7000 files into `git status`; the compiled server binary (38MB) was untracked
# too, so a `git add .` would have committed it.
node_modules/
server/servermesrv
server/deployzysrv
video/out/
video/node_modules/.remotion/

# texis-bot is a separate project that merely lives in this directory. It holds
# live Telegram bot tokens hardcoded in source (bot.py, module/ActionMode.py),
# a .env, user records and ~200 payment receipts. It must not enter this repo's
# history — give it its own repository.
texis-bot/
